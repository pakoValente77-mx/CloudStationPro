[timescaledb]
host = atlas16.ddns.net
port = 5432
user = postgres
password = ***REDACTED-PG-PASSWORD***
database = mycloud_timescale

[sql_server_remote]
server = atlas16.ddns.net
port = 1433
database = IGSCLOUD
user = sa
password = ***REDACTED-SQL-PASSWORD***

[sql_server_local]
server = atlas16.ddns.net
port = 1433
database = IGSCLOUD
user = sa
password = ***REDACTED-SQL-PASSWORD***

[lrgs]
# Credenciales por defecto para LRGS
default_user = cilasur
default_password = ksip-CWNG-05
# Hosts LRGS separados por coma
hosts = lrgseddn1.cr.usgs.gov,lrgseddn2.cr.usgs.gov,lrgseddn3.cr.usgs.gov
# Credenciales especiales por host (formato: host=user:password)
credentials_10.41.75.100 = test:test

[paths]
# Ruta por SO (el script detecta automáticamente)
mis_output_dir_windows = C:\IGSCLOUD\Datos\GOES
mis_output_dir_mac = /Users/subgerenciagrijalva/CFE/CloudStation/Datos/GOES

[settings]
# Modo de SQL Server a usar: 'remoto' o 'local'
sql_mode = remoto
# Intervalo en minutos para recargar estaciones/sensores desde SQL Server
reload_interval_minutes = 5
